---
# defaults file for skupper_link
#
# it expects that the target host has a generated_token variable populated by skupper_token role
#

#
# list of links to be created
# each item in the list holds a dictionary that contains:
# host, name and cost
# it expects that the target 'host' has an ansible variable named generated_token
#
skupper_link_links: []
