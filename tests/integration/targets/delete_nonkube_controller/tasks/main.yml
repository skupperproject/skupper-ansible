---
- name: Gathering facts
  ansible.builtin.gather_facts:
- name: Delete the system controller
  skupper.v2.controller:
    action: uninstall
  failed_when: false
  environment:
    DBUS_SESSION_BUS_ADDRESS: "{{ 'unix:path=/run/user/' + ansible_real_user_id | string + '/bus' }}"
