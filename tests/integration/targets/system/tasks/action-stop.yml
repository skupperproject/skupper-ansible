---
- name: Run the stop action
  skupper.v2.system:
    action: stop
    namespace: "{{ namespace }}"
  register: _system
- name: Validate namespace stopped
  include_tasks: validate-namespace-stopped.yml
